<%= form_for post.comments.build, url: post_comments_path(post), remote: true do |f| %>

    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.text_field :body, placeholder: 'Escreva um comentário...', autocomplete: 'off', required: true %>
    <%= f.submit 'Enviar' %>

<% end %>