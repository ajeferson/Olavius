<% if posts.any? %>

    <% posts.each do |post| %>
        <%= render partial: 'posts/post', locals: {post: post} %>
    <% end %>

<% else %>

    <%= render partial: 'layouts/none', locals: {message: 'Nenhuma publicação'} %>

<% end %>