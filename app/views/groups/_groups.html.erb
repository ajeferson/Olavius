<% if groups.any? %>
    <ul>
      <% groups.each do |group| %>
          <li><%= link_to group.name, group %></li>
      <% end %>
    </ul>
<% else %>
    <%= render 'layouts/none', locals: {message: 'Nenhum grupo'} %>
<% end %>